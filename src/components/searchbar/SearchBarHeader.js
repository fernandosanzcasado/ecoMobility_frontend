import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function SearchBarHeader() {
  return (
    <View>
      <Text>SearchBarHeader</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
